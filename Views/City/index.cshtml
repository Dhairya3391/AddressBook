@model UserModel

@using (Html.BeginForm())
{
    @Html.LabelFor(e=>e.email)
    @Html.TextBoxFor(e=>e.email)
    
    @Html.LabelFor(e=>e.password)
    @Html.TextBoxFor(e=>e.password)

    @Html.LabelFor(e=>e.confirmPassword)
    @Html.TextBoxFor(e=>e.confirmPassword)

}